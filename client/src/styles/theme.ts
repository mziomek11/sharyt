export type Theme = {
  primaryColor: string;
  secondaryColor: string;
  chatPadding: string;
};

export const theme: Theme = {
  primaryColor: "#bf0000",
  secondaryColor: "#FFFECB",
  chatPadding: "0.5em",
};
