export type Theme = {
  colors: {
    primary: string;
    light: string;
    dark: string;
  };
};

export const theme: Theme = {
  colors: {
    primary: "#bf0000",
    light: "rgba(255, 255, 255, 0.95)",
    dark: "rgba(0, 0, 0, 0.95)",
  },
};
